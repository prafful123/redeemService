describe("Check Elgibility for Rewards", function(){
    it(customers.indexOf(customerAccountNumber) !== -1, function(){
   
    it(eligibilityService[customerAccountNumber].length != 0,function(){
        expect(res) = tobe(channels[portfolio]);
    })
    it(eligibilityService[customerAccountNumber].length == 0,function(){
        expect(res) = tobe(response['CUSTOMER_INELIGIBLE']);
    })
    it(customers.indexOf(customerAccountNumber) == -1,function(){
        expect(res) = toBe(response['INVALID_ACCOUNT_NUMBER_EXCEPTION']);
    })
  })
})

